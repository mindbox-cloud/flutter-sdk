name: analyze_and_test

on:
  pull_request:
    branches: [ develop ]

jobs:

  platform_interface_flutter_test:
    name: platform_interface running analyze and tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v1
        with:
          java-version: "12.x"
      - uses: subosito/flutter-action@v2
        with:
          channel: "stable"
      - name: Add publish_to tag
        run: |
         cd mindbox_platform_interface && echo "publish_to: 'none'" | cat - pubspec.yaml > temp && mv temp pubspec.yaml
      - run: cd mindbox_platform_interface && flutter pub get
      - run: cd mindbox_platform_interface && flutter analyze
      - run: cd mindbox_platform_interface && flutter test
      
  android_component_flutter_test:
    name:  android_component running analyze and tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v1
        with:
          java-version: "12.x"
      - uses: subosito/flutter-action@v2
        with:
          channel: "stable"
      - name: Add publish_to tag
        run: |
         cd mindbox_android && echo "publish_to: 'none'" | cat - pubspec.yaml > temp && mv temp pubspec.yaml
      - name: Replace version to local path
        run: |
         cd mindbox_android && sed -i 's/^mindbox_platform_interface.*/mindbox_platform_interface:\n  path: ..\/mindbox_platform_interface/' pubspec.yaml
      - run: cd mindbox_platform_interface && flutter pub get
      - run: cd mindbox_platform_interface && flutter analyze
      - run: cd mindbox_platform_interface && flutter test   
      
#   flutter_test:
#     name: ${{ matrix.package }} running analyze and tests
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         package:
#           - 'mindbox'
#           - 'mindbox_android'
#           - 'mindbox_ios'
#           - 'mindbox_platform_interface'
#       fail-fast: false
#     steps:
#       - uses: actions/checkout@v3
#       - uses: actions/setup-java@v1
#         with:
#           java-version: "12.x"
#       - uses: subosito/flutter-action@v2
#         with:
#           channel: "stable"
#       - run: cd ${{ matrix.package }} && flutter pub get
#       - run: cd ${{ matrix.package }} && flutter analyze
#       - run: cd ${{ matrix.package }} && flutter test
